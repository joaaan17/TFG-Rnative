/**
 * Reexport desde `shared` para el backend (una sola fuente de verdad).
 */
export {
  DIVISION_THRESHOLDS,
  getDivisionFromExperience,
  toExperienceNumber,
} from '../../../../../shared/constants/divisions';
